{
  "name": "realtydirect",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:api": "npm install && cd packages/lib && npm run build && cd ../ai && npm run build && cd ../../ && mkdir -p apps/api/node_modules/@realtydirect && cp -r packages/lib/* apps/api/node_modules/@realtydirect/ && cp -r packages/ai/* apps/api/node_modules/@realtydirect/ && cd apps/api && npm run build && npx prisma generate",
    "build:web": "npm install && cd packages/lib && npm run build && cd ../../ && mkdir -p apps/web/node_modules/@realtydirect && cp -r packages/lib/* apps/web/node_modules/@realtydirect/ && cd apps/web && npm run build",
    "start:api": "npm run start:prod --workspace=api",
    "start:web": "npm run start:prod --workspace=web",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=web",
    "dev:api": "turbo run dev --filter=api",
    "dev:mobile": "turbo run dev --filter=mobile",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
